{
  "name": "holylabs-chat-sdk-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for @holylabs/chat-sdk (core), @holylabs/chat-sdk-rn (React Native widget), and @holylabs/chat-sdk-web (Web widget).",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
